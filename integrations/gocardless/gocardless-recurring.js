import { supabase } from "../../supabase.js";
const CREATE_RECURRING_URL="https://ncitqqhxaxjhepfsnltk.supabase.co/functions/v1/gocardless-create-recurring";
async function getProfileCustomer(){const profile=document.getElementById("editCustomer");if(!profile)return null;const detailList=document.querySelector(".detail-list"),values={};if(detailList)detailList.querySelectorAll(":scope > div").forEach(item=>{const label=item.querySelector("span")?.textContent?.trim().toLowerCase(),value=item.querySelector("strong")?.textContent?.trim();if(label&&value)values[label]=value;});const name=values.name||document.getElementById("pageTitle")?.textContent?.trim();if(!name)return null;const{data,error}=await supabase.from("customers").select("id,name,email,address_line1,address_line2,city,postcode").eq("name",name).limit(1).maybeSingle();if(error){console.error("Unable to find profile customer",error);return null;}return data;}
async function getDirectDebitStatus(customerId){const{data:customerLink}=await supabase.from("gocardless_customers").select("gocardless_customer_id").eq("customer_id",customerId).maybeSingle();if(!customerLink?.gocardless_customer_id)return"none";const{data:mandate}=await supabase.from("gocardless_mandates").select("id,status,cancelled_at,updated_at").eq("customer_id",customerId).order("updated_at",{ascending:false}).limit(1).maybeSingle();const{data:subscription}=await supabase.from("gocardless_subscriptions").select("status,cancelled_at,updated_at").eq("customer_id",customerId).order("updated_at",{ascending:false}).limit(1).maybeSingle();const ss=String(subscription?.status||"").toLowerCase(),ms=String(mandate?.status||"").toLowerCase();if(["cancelled","canceled","finished","failed","expired"].includes(ss)||["cancelled","canceled","failed","expired"].includes(ms))return"cancelled";if(["active","created","pending_customer_approval","pending_submission"].includes(ss))return ss==="active"?"active":"pending";if(ms&&ms!=="active")return"pending";if(ms==="active")return"active";return"none";}
function createSetupButton(customer,label="Set Up New Direct Debit"){const button=document.createElement("button");button.type="button";button.className="btn btn-secondary gocardless-recurring-setup-btn";button.textContent=label;button.addEventListener("click",()=>openRecurringSetup(customer));return button;}
function buildDirectDebitControls(customer,status){const wrapper=document.createElement("div");wrapper.className="gocardless-recurring-controls";wrapper.style.display="inline-flex";wrapper.style.gap="8px";wrapper.style.alignItems="center";if(status==="active"){const b=document.createElement("button");b.type="button";b.className="btn btn-secondary gocardless-recurring-btn";b.textContent="✓ Direct Debit Active";b.disabled=true;wrapper.appendChild(b);}else if(status==="cancelled"){const b=document.createElement("button");b.type="button";b.className="btn btn-secondary gocardless-recurring-btn";b.textContent="✕ Direct Debit Cancelled";b.disabled=true;wrapper.appendChild(b);wrapper.appendChild(createSetupButton(customer));}else if(status==="pending"){const b=document.createElement("button");b.type="button";b.className="btn btn-secondary gocardless-recurring-btn";b.textContent="Direct Debit Setup Pending";b.disabled=true;wrapper.appendChild(b);}else wrapper.appendChild(createSetupButton(customer,"Monthly Direct Debit"));return wrapper;}
async function openRecurringSetup(customer){const monthly=window.prompt("Monthly amount (£)","10");if(monthly===null)return;const amount=Math.round(Number(monthly)*100);if(!Number.isInteger(amount)||amount<1){alert("Please enter a valid monthly amount.");return;}const day=window.prompt("Payment day (1-28)","1");if(day===null)return;const dayOfMonth=Number(day);if(!Number.isInteger(dayOfMonth)||dayOfMonth<1||dayOfMonth>28){alert("Payment day must be between 1 and 28.");return;}const description=window.prompt("Description","Monthly Direct Debit");if(description===null)return;const{data:{session}}=await supabase.auth.getSession();if(!session?.access_token){alert("Please sign in again before setting up Direct Debit.");return;}try{const response=await fetch(CREATE_RECURRING_URL,{method:"POST",headers:{Authorization:`Bearer ${session.access_token}`,"Content-Type":"application/json"},body:JSON.stringify({customer_id:customer.id,amount,day_of_month:dayOfMonth,description:description.slice(0,255)})}),result=await response.json();if(!response.ok||!result.authorisation_url)throw new Error(result.error||"Unable to start Direct Debit setup");window.location.href=result.authorisation_url;}catch(error){console.error(error);alert(`Could not start the Direct Debit Setup: ${error.message}`);}}
async function addProfileDirectDebitAction(){const customer=await getProfileCustomer();if(!customer)return;const status=await getDirectDebitStatus(customer.id),controls=buildDirectDebitControls(customer,status),editButton=document.getElementById("editCustomer");if(!editButton)return;const existing=document.querySelector(".gocardless-recurring-controls, .gocardless-recurring-btn");if(existing)existing.remove();editButton.parentElement?.appendChild(controls);}
function removeListDirectDebitButtons(){document.querySelectorAll(".customer-row .gocardless-recurring-controls, .customer-row .gocardless-recurring-btn").forEach(button=>button.remove());}function refreshDirectDebitUI(){removeListDirectDebitButtons();addProfileDirectDebitAction().catch(error=>console.error("Direct Debit UI error",error));}const observer=new MutationObserver(()=>{clearTimeout(window.__jobpilotGcRefresh);window.__jobpilotGcRefresh=setTimeout(refreshDirectDebitUI,100);});observer.observe(document.body,{childList:true,subtree:true});window.addEventListener("load",refreshDirectDebitUI);setTimeout(refreshDirectDebitUI,250);
